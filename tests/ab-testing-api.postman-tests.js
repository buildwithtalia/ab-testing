// Postman test scripts for AB Testing API endpoints
// Each script validates response status, key properties, and error handling
// Endpoints covered: Get all experiments, Track conversion event, Create User Segment, Delete experiment, Assign user to variant, Create a new experiment, Get experiment by ID, Health check endpoint, Update experiment

// Example structure for each endpoint (replace with actual scripts as generated by Postman):

// Get all experiments
pm.test("Status code is 200", function () {
    pm.response.to.have.status(200);
});
pm.test("Response has experiments array", function () {
    var jsonData = pm.response.json();
    pm.expect(jsonData).to.have.property('experiments');
    pm.expect(jsonData.experiments).to.be.an('array');
});

// ... (repeat for each endpoint)

// Note: 'Get experiment results' test script could not be generated due to missing request.
